# App PostgreSQL (host port — for local dev outside Docker)
APP_DB_HOST=localhost
APP_DB_PORT=5433
APP_DB_NAME=saruk_db
APP_DB_USER=postgres
APP_DB_PASSWORD=postgres

# Airflow metadata PostgreSQL (host port — for local inspection only)
AIRFLOW_DB_HOST=localhost
AIRFLOW_DB_PORT=5438
AIRFLOW_DB_NAME=airflow
AIRFLOW_DB_USER=airflow
AIRFLOW_DB_PASSWORD=airflow
